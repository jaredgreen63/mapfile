# Upstream inventory source. Override the configured default without editing code.
INVENTORY_SOURCE_URL="https://www.escudechevrolet.com/"

# Adapter: "sitemap-jsonld" (default crawl), "feed" (authorized inventory feed), or "demo".
INVENTORY_SOURCE_ADAPTER="sitemap-jsonld"

# For the "feed" adapter: direct URL to an authorized CSV / XML / JSON inventory feed.
INVENTORY_FEED_URL=""

# Optional contact routing for the lead form.
LEAD_NOTIFICATION_EMAIL=""
